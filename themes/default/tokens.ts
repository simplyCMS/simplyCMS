import type { DesignTokens } from 'simplycms/themes/types';

/**
 * Токени default-теми — світлий монохром із латунним акцентом.
 *
 * Джерело значень — НЕ око, а вимір: локальні артефакти інспекцій
 * `docs/design-references/` (4 інспекції + затверджений
 * `tokens-proposal.json`; поза git з 2026-08-17). Кожне відхилення від
 * машинної пропозиції записане там у `decisions` з підставою; нижче —
 * короткі маркери, щоб не тримати обґрунтування лише в JSON.
 *
 * `applyTokens` розкладає цей обʼєкт у наявні semantic-змінні shadcn, тому
 * власного CSS тема не постачає. Темних перекриттів немає свідомо:
 * `darkDetected: false` на всіх чотирьох сторінках референсу — сайт світлий,
 * і при dark-емуляції палітра не змінюється.
 */
export const tokens: DesignTokens = {
  // Полотно сторінки — #f5f5f5, картки — чисто білі. Саме так, а не навпаки:
  // машина віддала #ffffff у `background` за частотою (99 елементів-карток),
  // але площею полотно вигравало вдесятеро (10.2 млн px² на головній).
  background: '0 0% 96%',
  foreground: '0 0% 4%',

  card: '0 0% 100%',
  'card-foreground': '0 0% 4%',

  popover: '0 0% 100%',
  'popover-foreground': '0 0% 4%',

  // Первинна дія — не чорна, а вугільна: усі три CTA референсу («Shop
  // Watches», «Add to cart», «Subscribe») мають фон `#000` з альфою 0.80,
  // що над полотном дає #313131, над білим — #333333. Контраст із білим
  // текстом 12.63:1.
  primary: '0 0% 20%',
  'primary-foreground': '0 0% 100%',

  // Другорядна дія — біла кнопка з рамкою («View Best Sellers», «Wishlist»).
  secondary: '0 0% 100%',
  'secondary-foreground': '0 0% 20%',

  muted: '220 14% 96%',
  // 🔴 Свідомий відхід від референсу: його #6a7282 не тягне WCAG AA 4.5:1
  // на полотні — 4.44:1 (на білій картці 4.84:1, тобто провалюється саме
  // над #f5f5f5, а картка проходить). #4a5565 — теж виміряний колір
  // референсу, але 6.93:1 на полотні й 7.56:1 на картці. Робимо design
  // transfer, не піксельну копію.
  // Числа перевимiряно в прогоні №2 (2026-08-18) тим самим `contrastRatio`
  // зі скрипта інспекції — попередній коментар називав 4.39:1 «на картці»,
  // тобто і значення, і поверхню.
  'muted-foreground': '216 15% 34%',

  // Латунь бейджа «BEST SELLER». Foreground саме чорнильний: білий на цій
  // латуні дає 2.78:1 — провал AA.
  accent: '40 43% 52%',
  'accent-foreground': '0 0% 4%',

  // Зелений бейдж знижки «Save 18%» — виміряно як пару фон/текст.
  success: '138 76% 97%',
  'success-foreground': '145 100% 25%',

  // Жовтий зірок рейтингу. Білий текст на ньому — 1.72:1, тому чорнило.
  warning: '44 100% 50%',
  'warning-foreground': '0 0% 4%',

  // Деструктивного стану на жодній із чотирьох сторінок референсу не
  // трапилось — не вигадуємо, лишаємо значення попередньої default-теми.
  destructive: '0 72% 51%',
  'destructive-foreground': '0 0% 100%',

  // Рамка — #e5e7eb (виміряно на borderTopColor обведених кнопок), а не
  // #f3f4f6: те значення — поверхня, і машина сплутала його з рамкою.
  border: '220 13% 91%',
  input: '220 13% 91%',
  ring: '0 0% 20%',

  // 22px — радіус панелей референсу (вкладені плитки 18px, тумби 14px,
  // кнопки — pill). Єдине, де машина й вимір збіглися точно.
  radius: '1.375rem',

  // Одна гарнітура на все: контраст заголовків референс тримає вагою й
  // трекінгом (h1 64px/700/-1.6px), а не другою сімʼєю. Моно (Geist Mono)
  // токеном бути не може — контракт свідомо не має `font-mono`, — тож воно
  // живе літеральним стеком у компонентах теми як акцентна гарнітура даних.
  'font-sans': 'Geist, system-ui, sans-serif',
  'font-heading': 'Geist, system-ui, sans-serif',
};
