-- 0002_grants.sql — привілейна поверхня як код (рішення B5″).
--
-- 🔴 ПЛЕЙСХОЛДЕР: наповнює Task 4 плану В2-К1а. Файл існує вже зараз, щоб
-- порядок канону був зафіксований до того, як зʼявиться вміст: гранти
-- потребують НАЯВНИХ таблиць (`0001_init.sql`), але мусять лягти ДО сіду
-- (`0003_seed.sql`) — інакше номер довелося б міняти заднім числом, а
-- міграції immutable.
--
-- Що тут буде (Task 4):
--   • явні GRANT-и по таблицях і сиквенсах для `app_user` і `app_admin`;
--   • `app_runtime` лишається БЕЗ прямих грантів — це і є fail-closed:
--     забутий `SET LOCAL ROLE` падає з `permission denied`, а не мовчки
--     обходить RLS;
--   • гейт парності привілеїв (`information_schema.role_table_grants` +
--     EXECUTE-ACL) у контурі `pnpm test:schema`.
--
-- Причина, чому це окремий файл, а не «як вийде»: живий аудит показав нуль
-- GRANT-ів у репо — 280×2 table-grants на проді були неявним дефолтом
-- платформи, який ніхто свідомо не обирав. Привілеї мусять бути текстом
-- під ревʼю, як і схема.
