/**
 * Барель теки схеми — щоб документований специфікатор `simplycms/schema`
 * резолвився і ВСЕРЕДИНІ монорепо (Task 7, В2-К1а).
 *
 * 🔴 Причина існування файлу вузька й механічна. В опублікованому пакеті
 * `simplycms/schema` веде через `exports`-мапу прямо в `schema.ts`, а в
 * монорепо той самий рядок резолвиться аліасом `simplycms/*` →
 * `packages/simplycms/src/*`, тобто в ТЕКУ — і без `index.ts` падає
 * (`Cannot find package`). Поки схему ніхто не імпортував, розрив був
 * невидимий; перший же споживач (`src/auth`) на нього наступив.
 *
 * Поверхня тут нічого не додає й не звужує: це рівно `schema.ts`, який сам
 * реекспортує `./auth` і `./media`. Entry-точкою tsup барель НЕ стає —
 * `dist/` лишається дзеркалом `exports`-мапи, тож у магазині модуль схеми
 * як був одним інстансом, так і лишається.
 */
export * from './schema';
