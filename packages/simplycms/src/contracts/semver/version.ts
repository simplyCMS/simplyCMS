/**
 * Версія ядра SimplyCMS у рантаймі.
 *
 * 🔴 ЗГЕНЕРОВАНЕ ЗНАЧЕННЯ: константу оновлює `scripts/release/bump.mjs`
 * разом із бампом манифестів (модель версій синхронна — усі публіковані
 * пакети мають одну версію, тож version БУДЬ-ЯКОГО з них = версія ядра).
 * Руками не редагувати; парність із `package.json` стереже
 * `packages/simplycms/src/contracts/__tests__/semver.test.ts`.
 *
 * Чому константа, а не читання `package.json` у рантаймі: значення потрібне
 * і в КЛІЄНТСЬКОМУ бандлі (перевірка `engines.simplycms` тем/плагінів на
 * bootstrap), де файлової системи немає, а JSON-імпорт манифесту тягнув би
 * в бандл весь `package.json`.
 */
export const CORE_VERSION = '0.3.0';
