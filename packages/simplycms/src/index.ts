// Корінь флагманського пакета `simplycms` — НАВМИСНО порожній вхід.
//
// Фреймворк споживається виключно субшляхами `exports`-мапи
// (`simplycms/contracts`, `simplycms/ui`, `simplycms/admin`, …): рівно так,
// як до К0 споживалися окремі пакети, один до одного. Барель-корінь із
// реекспортами звів би всі тіри T0→T5 в ОДИН граф модулів — імпорт будь-чого
// тягнув би за собою і серверні клієнти, і адмінку, і теми, вбиваючи
// tree-shaking і межу тірів, яку стереже `eslint.tier-zones.mjs`.
//
// Вхід лишається оголошеним, щоб `import 'simplycms'` резолвився, а пакет
// збирався й проходив гейти пакування нарівні з рештою.
export {};
